# UniAD (Planning-oriented Autonomous Driving) 论文关键信息提取

> 论文标题: Planning-oriented Autonomous Driving (UniAD)
> 作者: Yihan Hu, Jiazhi Yang, Li Chen, Keyu Li, Chonghao Sima, Xizhou Zhu, Siqi Chai, Senyao Du, Tianwei Lin, Wenhai Wang, Lewei Lu, Xiaosong Jia, Qiang Liu, Jifeng Dai, Yu Qiao, Hongyang Li
> 机构: OpenDriveLab & OpenGVLab (上海人工智能实验室), 武汉大学, 商汤科技
> 发表: CVPR 2023 | arXiv: 2212.10156
> 代码: https://github.com/OpenDriveLab/UniAD

---

## 一、核心思想与动机

### 1.1 问题背景
- 现代自动驾驶系统由**模块化任务**按顺序组成：感知 → 预测 → 规划
- 现有方案存在两类问题：
  - **(a) 独立模型方案**：各任务使用独立模型，存在信息丢失、误差累积、特征不对齐的问题
  - **(b) 多任务学习(MTL)方案**：共享主干网络+分离任务头，可能存在"负迁移"(negative transfer)
  - **(c.1) 端到端直接规划**：直接从传感器数据预测轨迹，无需显式感知/预测监督，但安全性不足、可解释性差
  - **(c.2) 部分组件端到端**：包含一些感知和预测模块，但不够全面

### 1.2 UniAD的核心主张
- **以规划为导向(Planning-oriented)**: 自动驾驶系统应该以最终目标（规划）为导向来设计，前置任务应当有效地服务于规划
- 这不是简单的任务堆叠，而是通过**query-based设计**连接所有节点，实现任务间的有效协调
- 是**首个全面研究感知、预测、规划多种任务联合协作**的工作

---

## 二、整体架构

### 2.1 架构概览
UniAD包含4个基于Transformer Decoder的感知与预测模块，以及1个最终的规划器：

```
多相机图像 → 特征提取器 → BEV特征(B) → [TrackFormer → MapFormer] → MotionFormer → OccFormer → Planner
                    ↑                                                                        ↑
                    └── BEV Encoder (BEVFormer) ────────────────────────────────────────────┘
```

### 2.2 核心设计理念：Query作为统一接口
- 所有模块通过**Query**连接，而非传统的边界框表示
- Query的**感受野更大**，可以缓解上游预测的复合误差
- Query灵活地建模多种交互关系（如多智能体之间的关系）

---

## 三、各模块详解

### 3.1 感知: TrackFormer (检测+跟踪)
- **功能**: 联合执行3D目标检测和多目标跟踪(MOT)，无需不可微的后处理
- **设计**:
  - 借鉴MOTR，使用两类query：**检测query**(新生目标)和**跟踪query**(已跟踪目标)
  - 通过Self-Attention模块聚合时序信息
  - 包含N层，最终输出QA提供Na个有效agent的特征
  - **引入一个特殊的ego-vehicle query**，显式建模自车，用于下游规划
- **指标**: AMOTA, AMOTP, IDS, Recall

### 3.2 感知: MapFormer (在线建图)
- **功能**: 基于2D全景分割方法Panoptic SegFormer，在线分割道路元素
- **类别**: lanes(车道线), dividers(分隔线), crossings(人行横道)作为"things"；drivable area(可行驶区域)作为"stuff"
- **输出**: 地图query QM，传递给MotionFormer进行agent-map交互
- **指标**: IoU-lane, IoU-road

### 3.3 预测: MotionFormer (运动预测)
- **功能**: 以场景为中心的方式预测所有agent的多模态未来轨迹(top-k)
- **三种交互建模**:
  1. **Agent-Agent交互**: 自注意+交叉注意力 → Qa
  2. **Agent-Map交互**: 交叉注意力 → Qm
  3. **Agent-Goal Point交互**: 可变形注意力 → Qg
- **Motion Query设计** (Qpos + Qctx):
  - Qpos集成四类位置信息：场景级锚点(Is)、智能体级锚点(Ia)、当前位置、预测目标点
  - 场景级锚点：k-means聚类全局运动统计
  - 智能体级锚点：k-means聚类局部意图
  - 粗到精(coarse-to-fine)逐层优化
- **非线性优化(NLO)**: 考虑到上游感知的不确定性，使用非线性平滑器调整目标轨迹，使其在物理上可行（约束jerk、曲率、曲率变化率、加速度、横向加速度）
- **输出**: 预测轨迹 {x̂i,k}、ego-vehicle query的特征
- **指标**: minADE, minFDE, MR, EPA, minFDE-AP

### 3.4 预测: OccFormer (占用预测)
- **功能**: 预测多步未来的占用网格图，保留agent身份
- **核心创新**: 同时包含场景级和智能体级语义
- **设计**:
  - 由To个时序block组成(To=5，含当前帧和未来4帧)
  - 每个block：下采样→自注意力→交叉注意力(带mask)→上采样→残差连接
  - Agent特征Gt：融合track query QA、位置PA、运动query QX
  - **像素-智能体交互(Pixel-Agent Interaction)**: 密集特征作为query，实例级特征作为key/value
  - **注意力掩码(Attention Mask)**: 限制每个像素只关注占据它的agent
  - **实例级占用**: 通过矩阵乘法 Ô_A^t = U^t · F_dec^t 产生
- **指标**: IoU-near, IoU-far, VPQ-near, VPQ-far

### 3.5 Planner (规划器)
- **输入**: ego-vehicle query + 导航指令嵌入(左转/右转/直行) → plan query
- **流程**: plan query交叉注意力BEV特征 → 解码为未来轨迹τ̂
- **碰撞避免优化**(仅推理时): 使用牛顿法优化，代价函数包含：
  - L2损失：将轨迹拉向原始预测
  - 碰撞项：将轨迹推离占用网格
- **指标**: L2误差(m), 碰撞率(%)

---

## 四、训练策略

### 4.1 两阶段训练
1. **阶段1** (6 epochs): 联合训练感知部分(跟踪+建图)，冻结图像骨干网络
2. **阶段2** (20 epochs): 端到端训练所有模块(感知+预测+规划)，冻结图像骨干和BEV编码器

### 4.2 共享匹配
- 使用匈牙利算法进行二分图匹配
- 跟踪模块的匹配结果在运动和占用模块中**复用**，实现从历史跟踪到未来运动的一致建模

### 4.3 损失函数
| 模块 | 损失函数 |
|------|---------|
| 跟踪 | Focal Loss + L1 Loss |
| 建图 | Focal Loss + L1 Loss + Dice Loss + GIoU Loss |
| 运动预测 | 分类分数损失 + 负对数似然损失(NLL) |
| 占用预测 | 二元交叉熵 + Dice Loss |
| 规划 | 模仿L2损失 + 碰撞损失(IoU-based) |

---

## 五、关键实验结果

### 5.1 联合消融实验结果 (Table 2)
- **感知→预测**: 同时加入跟踪和建图模块，运动预测大幅提升：minADE -9.7%, minFDE -12.9%, MR -2.3%
- **预测协作**: 运动预测和占用预测同时加入，两者互相促进：minADE -3.5%, minFDE -5.8%, MR -1.3%
- **规划安全**: 仅当两个预测任务同时引入时，规划L2和碰撞率达到最优
- **vs 多任务学习(MTL)**: UniAD全面优于简单MTL方案：minADE -15.2%, minFDE -17.0%, MR -3.2%, avg.L2 -0.15m, avg.Col. -0.51%

### 5.2 各模块对比SOTA

| 模块 | UniAD | 对比方法 | 提升 |
|------|-------|---------|------|
| 跟踪 | AMOTA: 0.359 | MUTR3D: 0.294 | +6.5% |
| 建图 | IoU-lane: 31.3% | BEVFormer: 23.9% | +7.4% |
| 运动预测 | minADE: 0.71 | PnPNet: 1.15 | -38.3% |
| 占用预测 | IoU-near: 63.4% | FIERY: 59.4% | +4.0% |
| 规划 | avg.L2: 1.03m | ST-P3: 2.11m | -51.2% |
| 规划 | avg.Col: 0.31% | ST-P3: 0.71% | -56.3% |

### 5.3 模型规模
| 变体 | 骨干网络 | 参数量 | FLOPs | FPS |
|------|---------|--------|-------|-----|
| UniAD-S | ResNet-50 | 65.9M(仅检测) | ~1324G | 4.2 |
| UniAD-B | ResNet-101 | 125.0M(全模块) | ~1709G | 1.8 |
| UniAD-L | VoVNet-99 | - | - | - |

---

## 六、关键设计消融

### 6.1 MotionFormer各组件贡献 (Table 8)
- 旋转场景级锚点: minADE -15.8%, minFDE -11.2%
- Agent-Goal Point交互: 增强运动query的规划导向视觉特征
- Ego-vehicle query: 让周围agent受益于自车意图
- 非线性优化: minADE -5.0%, minFDE -8.4%

### 6.2 OccFormer各组件贡献 (Table 9)
- 无局部约束的交叉注意力反而略差于无注意力基线
- 占用引导的注意力掩码是关键：IoU-near +1.0%, VPQ-near +1.4%
- 复用mask特征(M^t)而非原始agent特征(G^t)来生成占用特征

### 6.3 Planner各组件贡献 (Table 10)
- BEV特征注意力：降低碰撞率
- 碰撞损失训练：进一步降低碰撞率
- 占用优化推理：碰撞率最低

---

## 七、重要定性结果

### 7.1 规划导向设计的鲁棒性
- 即使上游感知存在偏差（如目标漏检、大角度偏差），规划器仍能产生合理的轨迹
- 注意力掩码可视化显示：规划器即使在前置模块漏检的情况下，仍能关注到关键的漏检agent

### 7.2 导航指令的效果
- 不同的导航指令（左转/右转/直行）会显著改变规划轨迹
- 注意力集中在目标车道和与本车交互的关键agent上

### 7.3 失败案例
- 主要集中在**长尾场景**：大型拖车/挂车、夜间昏暗环境
- Trailer场景：检测模块无法准确估计前车尺寸和路旁车辆朝向
- 窄路场景：规划器对来车过度谨慎

---

## 八、局限性与未来方向

1. **计算开销大**：协调多任务系统需要大量计算资源，尤其是带时序历史的训练
2. **轻量化部署**：如何设计轻量级系统用于实际部署是未来方向
3. **更多任务融合**：是否引入深度估计、行为预测等更多任务，以及如何嵌入系统

---

## 九、技术亮点总结

1. **首个**全面融合检测、跟踪、建图、运动预测、占用预测、规划六大任务的端到端自动驾驶系统
2. **Query-based统一接口**：所有任务通过query连接，灵活建模交互，软化了复合误差
3. **规划导向设计哲学**：所有前置任务都服务于最终规划目标
4. **非线性目标轨迹优化**：考虑上游感知不确定性，使训练目标物理可行
5. **像素-智能体交互**：在占用预测中融合场景级和实例级知识
6. **碰撞避免优化**：推理时利用牛顿法+占用预测进行安全优化
7. **两阶段训练+共享匹配**：稳定训练，保持历史到未来的一致建模